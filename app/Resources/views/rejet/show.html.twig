{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rejet</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rejet.id }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ rejet.motif }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rejet_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rejet_edit', { 'id': rejet.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
