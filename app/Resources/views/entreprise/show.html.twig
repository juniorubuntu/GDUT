{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="panel panel-border panel-custom" style="background: #ebeff2">
            <header class="panel-heading">
                <h1 class="panel-title">Détail sur une entreprise</h1>
        </div>
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entreprise.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ entreprise.nom }}</td>
                </tr>
            </tbody>
        </table>
        <a class="btn btn-link" href="{{ path('entreprise_index') }}">Retourner à la liste</a>

        {#
<ul>
    <li>
        <a href="{{ path('entreprise_index') }}">Back to the list</a>
    </li>
    <li>
        <a href="{{ path('entreprise_edit', { 'id': entreprise.id }) }}">Edit</a>
    </li>
    <li>
        {{ form_start(delete_form) }}
        <input type="submit" value="Supprimer">
        {{ form_end(delete_form) }}
    </li>
</ul>
        #}
    </div>
{% endblock %}
