{% extends 'base.html.twig' %}

{% block body %}
    <h1>Planif</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planif.id }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ planif.duree }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('planif_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('planif_edit', { 'id': planif.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
