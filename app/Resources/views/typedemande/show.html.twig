{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="panel panel-border panel-custom" style="background: #ebeff2">
            <header class="panel-heading">
                <h1 class="panel-title">Détail sur un type de commande</h1>
        </div>
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ typeDemande.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ typeDemande.nom }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ typeDemande.description }}</td>
                </tr>
            </tbody>
        </table>
        <a class="btn btn-custom btn-primary btn-rounded waves-effect waves-lightr" href="{{ path('typedemande_index') }}">Retouner à la liste</a>
    </div>
    {#
    <li>
        <a href="{{ path('typedemande_edit', { 'id': typeDemande.id }) }}">Edit</a>
    </li>
    <li>
        {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </li>
    #}
{% endblock %}
